export enum UserRole {
  CUSTOMER = 'customer',
  DRIVER = 'driver',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  BLOCKED = 'blocked',
  DELETED = 'deleted',
  PENDING_VERIFICATION = 'pending_verification',
}
